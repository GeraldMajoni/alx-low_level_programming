#include "main.h"

/**
 * _islower - Entry into program
 * @c: character
 * Return: (1) (successful)
 */
int _islower(int c)
{
	int r;
	char c = 'a';

	r = _islower(int c);

	while (c <= 'z')
	{
		if (_putchar(r + '0') == _putchar(c))
		{
			return (1);
			_putchar('\n');
		}
		i++;
	}
}
